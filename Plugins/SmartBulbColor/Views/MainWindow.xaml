<Window x:Class="SmartBulbColor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartBulbColor.ViewModels"
        mc:Ignorable="d"
        Title="SmartBulbColor" Height="736" Width="685" ResizeMode="NoResize">
    <Window.DataContext>
        <local:MainWindowViewModel></local:MainWindowViewModel>
    </Window.DataContext>
    <Window.Background>
        <ImageBrush ImageSource="/SmartBulbColor;component/Resources/Background.jpg"/>
    </Window.Background>
    <Window.Resources>
        <Style x:Key="MainButtonStyle">
            <Setter Property="Control.Background" Value="Black"/>
            <Setter Property="Control.Width" Value="166"/>
            <Setter Property="Control.Height" Value="44"/>
            <Setter Property="Control.FontSize" Value="16"/>
            <Setter Property="Control.FontWeight" Value="Bold"/>
            <Setter Property="Control.Foreground" Value="#FF00F92D"/>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Control.Background" Value="Black"/>
            <Setter Property="Control.Width" Value="166"/>
            <Setter Property="Control.Height" Value="44"/>
            <Setter Property="Control.FontSize" Value="14"/>
            <Setter Property="Control.FontWeight" Value="Bold"/>
            <Setter Property="Control.Foreground" Value="#FF0035E8"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Opacity" Value="0.9"/>
            <Setter Property="Foreground" Value="#FF0035E8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Button x:Name="FindBulbs"  Command="{Binding FindBulbs}" Style="{StaticResource MainButtonStyle}" 
                Content="Find Devices" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,10,0,0"/>
        <Button x:Name="NormalLightButton" Command="{Binding TurnNormalLightON}" Style="{StaticResource MainButtonStyle}" 
                Content="Normal Light" HorizontalAlignment="Left" Margin="342,10,0,0" VerticalAlignment="Top" Width="166"/>
        <Button x:Name="AmbientLightButton" Command="{Binding ToggleAmbientLight}" Style="{StaticResource MainButtonStyle}" 
                Content="Ambient Light" HorizontalAlignment="Left" Margin="513,10,0,0" VerticalAlignment="Top" Width="166"/>
        <Button x:Name="ClearButton" Command="{Binding ClearConsole}" Style="{StaticResource MainButtonStyle}"
                Content="Clear Console" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="171,10,0,0"/>
        <ListBox x:Name="BulbList" ItemsSource="{Binding Bulbs}" SelectedItem="{Binding SelectedBulb}"                 
                 HorizontalAlignment="Left" Height="332" Margin="0,59,0,0" VerticalAlignment="Top" Width="166" FontSize="16" FontWeight="Bold">
        </ListBox>
        <Grid x:Name="PropertyList" DataContext="{Binding SelectedBulb}"
                  HorizontalAlignment="Left" Height="332" Margin="171,59,0,0" VerticalAlignment="Top" Width="508">
            <StackPanel Margin="329,0,10,0">
                <TextBlock Text="Current Bulb" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="ID"/>
                <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" Foreground="Black" Background="#FF1B5A8F"/>
                <TextBlock Text="Name"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Foreground="Black" Background="#FF1B5A8F"/>
                <TextBlock Text="Ip"/>
                <TextBox Text="{Binding Ip, UpdateSourceTrigger=PropertyChanged}" Foreground="Black" Background="#FF1B5A8F"/>
                <TextBlock Text="Brightness"/>
                <TextBox Text="{Binding Brightness, UpdateSourceTrigger=PropertyChanged}" Foreground="Black" Background="#FF1B5A8F"/>
                <TextBlock Text="ColorMode"/>
                <TextBox Text="{Binding ColorMode, UpdateSourceTrigger=PropertyChanged}" Foreground="Black" Background="#FF1B5A8F"/>
                <TextBlock Text="RGB"/>
                <TextBox Text="{Binding Rgb, UpdateSourceTrigger=PropertyChanged}" Foreground="Black" Background="#FF1B5A8F"/>
                <TextBlock Text="Temperature"/>
                <TextBox Text="{Binding  ColorTemperature, UpdateSourceTrigger=PropertyChanged}" Foreground="Black" Background="#FF1B5A8F"/>
                <TextBlock Text="Power"/>
                <TextBox Text="{Binding IsPowered, UpdateSourceTrigger=PropertyChanged}" Foreground="Black" Background="#FF1B5A8F"/>

            </StackPanel>
          
        </Grid>
        <TextBox Name="MainConsole" Text="{Binding Path=Logs}"  IsReadOnly="True" TextChanged="MainConsole_TextChanged"
                 HorizontalAlignment ="Right" Width ="679" Height="302" Padding ="10" VerticalAlignment="Bottom"/>
    </Grid>
</Window>
